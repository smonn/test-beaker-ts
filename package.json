{
  "name": "test-beaker",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "python ./src/contract.py",
    "build": "beaker generate ./src/app.json ./src/",
    "postbuild": "echo '// @ts-nocheck' | cat - ./src/hellobeaker_client.ts > /tmp/out && mv /tmp/out ./src/hellobeaker_client.ts",
    "test": "jest",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "@tsconfig/node16-strictest": "^1.0.3",
    "@types/jest": "^29.0.0",
    "@types/node": "^18.7.14",
    "beaker-ts": "^0.0.14",
    "jest": "^29.0.1",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "algosdk": "^1.19.1"
  }
}
